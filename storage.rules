rules_version = '2';

// Permissive storage rules for Firebase Storage Emulator testing
// All operations require authentication but allow full access to authenticated users
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
